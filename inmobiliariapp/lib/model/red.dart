import 'package:flutter/material.dart';

class Red {
  String red;
  IconData icono;
  TextEditingController controller;
  bool isPressed;

  Red(this.red, this.icono, this.controller, this.isPressed);
}
